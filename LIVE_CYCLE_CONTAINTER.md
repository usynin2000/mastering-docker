# 📦 Жизненный цикл контейнера Docker

> Жизненный цикл контейнера — это все состояния, через которые проходит контейнер от создания до удаления, и команды для управления этими переходами.
В отличие от традиционных приложений, контейнеры имеют чётко определённые состояния, которыми можно управлять. Это даёт тебе полный контроль над тем, как и когда приложение работает.


### 🔄 Состояния контейнера
Контейнер может находиться в одном из следующих состояний:
```shell
created → running → paused → stopped → deleted
            ↓          ↓         ↓
         (running)  (paused) (exited)
```


1️⃣ CREATED (создан)

Что это значит:
- Контейнер существует как объект Docker
- Файловая система подготовлена
- Но процессы внутри контейнера ещё не запущены


Как попасть в это состояние:
```shell
docker create --name mycontainer nginx
```

Проверить:
```shell
docker ps -a
# STATUS: Created
```

Когда это полезно:
- Хочешь предварительно подготовить контейнер (например, для автоматизации)
- Нужно создать контейнер, но запустить его позже

🎬 Для видео: Покажи, что docker create не запускает контейнер, а только готовит его.

Проверь docker ps -a и увидишь статус "Created".



2️⃣ RUNNING (запущен)

Что это значит:
- Главный процесс контейнера запущен и работает
- Контейнер активен, выполняет свои задачи
- Можно взаимодействовать с приложением внутри

Как попасть в это состояние:

Вариант 1: Запустить сразу
```shell
docker run -d --name mycontainer nginx
```

Вариант 2: Запустить созданный контейнер
```shell
docker create --name mycontainer nginx
docker start mycontainer
```

Проверить:
```shell
docker ps
# STATUS: Up 2 minutes
```

Важно понимать:
- Контейнер работает, пока работает его главный процесс (PID 1)
- Если главный процесс завершается → контейнер автоматически останавливается

🔍 Пример:
```shell
# Этот контейнер сразу завершится
docker run alpine echo "Hello"
# Почему? Потому что команда echo завершилась

# Этот контейнер будет работать
docker run -d alpine sleep 1000
# Процесс sleep держит контейнер запущенным
```

🎬 Для видео: Запусти nginx, покажи docker ps, зайди на localhost:8080, покажи логи docker logs.



3️⃣ PAUSED (приостановлен)

Что это значит:
- Все процессы внутри контейнера заморожены (suspended)
- Контейнер не использует CPU
- Память остаётся занятой
- Сетевые соединения остаются открытыми, но неактивны


Как попасть в это состояние:
```shell
docker pause mycontainer
```

Вернуть обратно в running:
```shell
docker unpause mycontainer
```

Проверить:
```shell
docker ps
# STATUS: Up 5 minutes (Paused)
```

Технически как это работает:
- Docker использует cgroups freezer
- Процессы не убиваются, а буквально "замораживаются" на уровне ядра Linux


Когда это полезно:
- Временная приостановка без потери состояния
- Освобождение CPU для других задач
- Дебаггинг (можно заморозить контейнер, чтобы изучить его состояние)

⚠️ Важно:
- Это не sleep или stop
- Память остаётся занятой
- Используется редко в production

🎬 Для видео:
```shell
# Запускаем контейнер, который печатает время каждую секунду
docker run -d --name timer alpine sh -c 'while true; do date; sleep 1; done'

# Смотрим логи
docker logs -f timer

# Приостанавливаем
docker pause timer
# Логи перестают обновляться

# Возобновляем
docker unpause timer
# Логи снова идут
```


4️⃣ STOPPED (EXITED) (остановлен)
Что это значит:
- Главный процесс завершён (gracefully или forcefully)
- Контейнер больше не работает
- Файловая система и данные в writable layer сохранены
- Можно снова запустить контейнер


Как попасть в это состояние:

Вариант 1: Graceful stop (корректная остановка)
```shell
docker stop mycontainer
```
- Отправляет сигнал SIGTERM (вежливая просьба завершиться)
- Ждёт 10 секунд
- Если процесс не завершился → отправляет SIGKILL (принудительное завершение)


Вариант 2: Принудительная остановка
```shell
docker kill mycontainer
```
- Сразу отправляет SIGKILL
- Процесс убивается мгновенно
- Опасно: данные могут не сохраниться корректно


Проверить:
```shell
docker ps -a
# STATUS: Exited (0) 2 minutes ago
# Или: Exited (137) если был kill
```

Exit codes (коды выхода):
- 0 — нормальное завершение
- 1 — ошибка приложения
- 137 — убит сигналом SIGKILL (обычно docker kill или OOM)
- 143 — завершён сигналом SIGTERM (docker stop)


Restart:
```shell
docker start mycontainer
```

🎬 Для видео:
```shell
# Graceful stop
docker run -d --name web nginx
docker stop web
# Смотри, что статус стал Exited

docker start web
# Снова работает

# Принудительная остановка
docker kill web
```


5️⃣ DELETED (REMOVED) (удалён)
Что это значит:
- Контейнер полностью удалён из системы
- Writable layer уничтожен → данные внутри контейнера потеряны
- Образ (image) остаётся

Как удалить:
```shell
# Удалить остановленный контейнер
docker rm mycontainer

# Принудительно удалить работающий контейнер
docker rm -f mycontainer

# Удалить все остановленные контейнеры
docker container prune
```

⚠️ Важно:
- После удаления контейнер невозможно восстановить
- Данные в volumes НЕ удаляются (они живут отдельно)
- Образ остаётся и можно создать новый контейнер


🎬 Для видео:
```shell
# Создаём контейнер
docker run -d --name test nginx

# Останавливаем
docker stop test

# Удаляем
docker rm test

# Проверяем
docker ps -a
# Контейнера нет
```




## 🔁 Restart Policies (Политики перезапуска)

Что это такое:
- Автоматические правила, определяющие, когда Docker должен перезапускать контейнер
- Очень важно для production — обеспечивает resilience (устойчивость)


Виды restart policies

| Policy | Описание | Когда использовать |
| --- | --- | --- |
| `no` | Никогда не перезапускать (по умолчанию) | Dev-среды, одноразовые задачи |
| `on-failure` | Перезапускать только при ошибке (exit code ≠ 0) | Приложения, которые могут упасть |
| `always` | Всегда перезапускать, даже если вручную остановлен | Production сервисы |
| `unless-stopped` | Всегда перезапускать, кроме ручной остановки | Production (рекомендуется) |


1️⃣ no (по умолчанию)
```shell
docker run --restart=no nginx
```

Поведение:
- Контейнер завершился → всё, больше не запускается
- Даже после перезагрузки Docker daemon


Когда использовать:
- Разработка
- Одноразовые скрипты
- Тестирование



2️⃣ on-failure
```shell
docker run --restart=on-failure nginx

# С ограничением попыток
docker run --restart=on-failure:5 nginx
```

Поведение:
- Если контейнер упал с ошибкой (exit code ≠ 0) → перезапускается
- Если завершился нормально (exit code = 0) → НЕ перезапускается
- Можно ограничить количество попыток


Пример:
```shell
# Этот контейнер будет перезапускаться
docker run -d --name fail --restart=on-failure alpine sh -c 'exit 1'

# Этот НЕ будет перезапускаться
docker run -d --name success --restart=on-failure alpine sh -c 'exit 0'
```

Когда использовать:
- Приложения, которые могут упасть (например, из-за сетевых проблем)
- Когда хочешь автоматически восстанавливаться после сбоев


3️⃣ always
```shell
docker run --restart=always nginx
```

Поведение:
- Контейнер завершился (по любой причине) → перезапускается
- Docker daemon перезапущен → контейнер запускается снова
- Даже если ты вручную остановил контейнер через docker stop, при перезагрузке Docker он снова запустится


Когда использовать:
- Production-сервисы, которые должны всегда работать
- Микросервисы, API, базы данных


⚠️ Нюанс:
Если ты сделаешь docker stop mycontainer, он остановится. 
Но при перезагрузке Docker daemon (или перезагрузке системы) контейнер снова запустится.


4️⃣ unless-stopped (рекомендуется для production)
```shell
docker run --restart=unless-stopped nginx
```
Поведение:
- Почти то же самое, что always
- НО: если ты вручную остановил контейнер через docker stop → он больше НЕ запустится автоматически


Разница с always:
```shell
# always
docker run -d --name web1 --restart=always nginx
docker stop web1
# Перезагрузка Docker → web1 снова запустится

# unless-stopped
docker run -d --name web2 --restart=unless-stopped nginx
docker stop web2
# Перезагрузка Docker → web2 НЕ запустится (потому что был остановлен вручную)
```


### 🎬 Практическая демонстрация для видео

Пример 1: Тестируем on-failure
```shell
# Создаём скрипт, который падает
docker run -d --name crasher --restart=on-failure:3 alpine sh -c '
  echo "Attempt $RANDOM"
  sleep 2
  exit 1
'

# Смотрим логи
docker logs -f crasher

# Смотрим статус
docker ps -a
# Увидишь, что контейнер перезапускается несколько раз
```


Пример 2: Тестируем unless-stopped
```shell
# Запускаем nginx
docker run -d --name web --restart=unless-stopped -p 8080:80 nginx

# Проверяем
curl localhost:8080

# Останавливаем вручную
docker stop web

# Перезапускаем Docker daemon (на macOS)
# Или просто делаем restart Docker Desktop

# Проверяем
docker ps -a
# web всё ещё остановлен (потому что unless-stopped)
```


Пример 3: Демонстрация всех состояний
```shell
# 1. Created
docker create --name demo nginx
docker ps -a  # STATUS: Created

# 2. Running
docker start demo
docker ps  # STATUS: Up

# 3. Paused
docker pause demo
docker ps  # STATUS: Up (Paused)

# 4. Unpause → Running
docker unpause demo
docker ps  # STATUS: Up

# 5. Stopped
docker stop demo
docker ps -a  # STATUS: Exited

# 6. Deleted
docker rm demo
docker ps -a  # Нет контейнера
```


### 🎯 Практические советы для production

✅ Best Practices

1. Используй unless-stopped для долгоживущих сервисов
```shell
   docker run -d --restart=unless-stopped --name api myapi:1.0
```

2. Graceful shutdown: убедись, что твоё приложение правильно обрабатывает SIGTERM
```python
   # Python example
   import signal
   import sys
   
   def signal_handler(sig, frame):
       print('Gracefully shutting down...')
       # Закрыть соединения, сохранить данные
       sys.exit(0)
   
   signal.signal(signal.SIGTERM, signal_handler)
```

3. Health checks: используй health checks для автоматического обнаружения проблем
```shell
   docker run -d \
     --restart=unless-stopped \
     --health-cmd="curl -f http://localhost/ || exit 1" \
     --health-interval=30s \
     nginx
```


4. Мониторинг: следи за перезапусками
```shell
   docker inspect --format='{{.RestartCount}}' mycontainer
```

📊 Итоговая схема
                 docker create
                      ↓
               ┌────────────┐
               │  CREATED   │
               └────────────┘
                      ↓ docker start
                      ↓
    ┌─────────────────────────────────┐
    │          RUNNING                │
    │  (главный процесс работает)     │
    └─────────────────────────────────┘
         ↓             ↑         ↑
    pause│         unpause    restart
         ↓             │         │
    ┌─────────┐       │         │
    │ PAUSED  │───────┘         │
    └─────────┘                 │
         │                      │
         │ stop/kill            │
         ↓                      │
    ┌─────────────┐             │
    │  STOPPED    │─────────────┘
    │  (Exited)   │
    └─────────────┘
         │ docker rm
         ↓
    ┌─────────────┐
    │  DELETED    │
    └─────────────┘